# PostgreSQL settings
POSTGRES_DB=your_db_name
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password

# Spring Boot DB variables
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JDBC URLs for each environment
DB_URL_DEV=jdbc:postgresql://db-recomandation-engine:5432/motor_recomendaciones
DB_URL_PROD=jdbc:postgresql://db-recomandation-engine:5432/motor_recomendaciones

# JWT security
JWT_SECRET_KEY=
ACCESS_TOKEN_EXPIRATION=